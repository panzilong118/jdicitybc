import * as React from 'react';

import PropTypes from 'prop-types';

import { ColumnProps } from './interface';

export default class Column extends React.Component {
    static propTypes = {...ColumnProps};
}
